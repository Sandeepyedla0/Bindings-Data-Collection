# Bindings-Data-Collection

This project collects the Pybind11 utilised repositories and meta data to local using GraphQL query.

run command - bash collect_data.sh
Main script logic in : gh_crawler.py
